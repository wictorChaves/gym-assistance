import { VanillaDirective } from './vanilla.directive';

describe('VanillaDirective', () => {
  it('should create an instance', () => {
    const directive = new VanillaDirective();
    expect(directive).toBeTruthy();
  });
});
